# Notes

This repository is intentionally separate from the existing Web, mobile, Express backend, and ML repositories.

The first implementation milestone should keep the agent service private behind the Express gateway. Web and iOS clients should not call MCP directly.

